services:
  - type: web
    name: ft-transcendence
    env: docker
    plan: free
    region: oregon
    branch: ali2
    dockerfilePath: ./Dockerfile.render
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
    autoDeploy: true
