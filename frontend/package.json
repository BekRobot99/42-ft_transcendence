{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist/",
    "build": "npm run clean && tsc",
    "watch": "tsc --watch",
    "dev": "concurrently \"npm run watch\" \"npx live-server --port=3000 --open=index.html --host=0.0.0.0\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.0.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
